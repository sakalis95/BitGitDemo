describe('My First Test', () => {
    it('should verify that true is false', () => {
        expect(true).to.equal(false);
    });
    it('should verify that true is true', () => {
        expect(true).to.equal(true);
    });
});